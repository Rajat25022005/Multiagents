# Root-level requirements.txt is intentionally minimal.
# The full backend requirements are in backend/requirements.txt
# The root Dockerfile is for the legacy orchestrator only.
fastapi
uvicorn
pydantic
pydantic-settings
python-dotenv
requests
